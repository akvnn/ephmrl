apiVersion: v1
kind: Service
metadata:
  name: {{ include "ephmrl.fullname" . }}
  labels:
    {{- include "ephmrl.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "ephmrl.selectorLabels" . | nindent 4 }}
