apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ephmrl.fullname" . }}-test-connection"
  labels:
    {{- include "ephmrl.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ephmrl.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
