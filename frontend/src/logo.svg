<svg width="300" height="80" viewBox="0 0 300 80" xmlns="http://www.w3.org/2000/svg">
    <g transform="translate(0, 10)">
        <!-- Top Bar (Solid) -->
        <rect x="0" y="0" width="12" height="60" rx="2" fill="#A9B3A9" />

        <!-- Middle Bar (Slightly shorter) -->
        <rect x="18" y="10" width="12" height="40" rx="2" fill="#A9B3A9" opacity="0.8" />

        <!-- Bottom Bar (Shortest) -->
        <rect x="36" y="20" width="12" height="20" rx="2" fill="#A9B3A9" opacity="0.6" />

        <!-- Dot (The remnant) -->
        <circle cx="60" cy="30" r="3" fill="#A9B3A9" opacity="0.4" />
    </g>

    <!-- Logotype -->
    <text x="90" y="52" font-family="'Inter', sans-serif" font-weight="600" font-size="38" letter-spacing="-1" fill="#2D2D2D">
        ephmrl
    </text>
</svg>
